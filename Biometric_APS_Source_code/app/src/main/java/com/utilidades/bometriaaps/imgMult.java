package com.utilidades.bometriaaps;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.ImageView;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

public class imgMult extends androidx.appcompat.widget.AppCompatImageView {
    public imgMult(@NonNull Context context) {
        super(context);
    }

    public imgMult(@NonNull Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public imgMult(@NonNull Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
